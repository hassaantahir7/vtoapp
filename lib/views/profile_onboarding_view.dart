import 'package:flutter/material.dart';

class ProfileOnboardingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Profile Onboarding'),
      ),
      body: Center(
        child: Text('Welcome to the profile onboarding screen!'),
      ),
    );
  }
}
